# Generated By: Claude Code (Claude Opus 4.6)
"""Entry point for the personal assistant GUI."""

import argparse
import logging
import sys


def main() -> None:
    parser = argparse.ArgumentParser(
        description="Personal Assistant — Claude-powered GUI"
    )
    parser.add_argument("--debug", action="store_true", help="enable debug logging")
    args = parser.parse_args()

    level = logging.DEBUG if args.debug else logging.INFO
    logging.basicConfig(
        level=level,
        format="%(asctime)s %(levelname)s %(name)s: %(message)s",
        stream=sys.stderr,
    )

    # Windows DPI awareness — must be called before Tk() is created
    if sys.platform == "win32":
        try:
            import ctypes

            ctypes.windll.shcore.SetProcessDpiAwareness(1)
        except Exception:
            pass

    # Deferred import — only after args parsed and logging configured
    from personal_assistant.controller import AppController

    app = AppController(debug=args.debug)
    app.run()


if __name__ == "__main__":
    main()
